export namespace Constants {
    export enum HabitStatus {
        INVALID = 'invalid',
        INCOMPLETE = 'INCOMPLETE',
        COMPLETE = 'COMPLETE',
        FAILED = 'FAILED',
        SKIPPED = 'SKIPPED',
    }

    export enum CompletionState {
        INVALID = 'INVALID',
        INCOMPLETE = 'INCOMPLETE',
        COMPLETE = 'COMPLETE',
        FAILED = 'FAILED',
        SKIPPED = 'SKIPPED',
    }
}
