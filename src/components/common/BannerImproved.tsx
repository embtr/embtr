import { View } from 'react-native';

interface Props {
    name: string;
}

export const BannerImproved = ({ name }: Props) => {
    return (
        <View>
            <View></View>
            <View></View>
            <View></View>
        </View>
    );
};
